/**
 * @file types.js
 * @brief Enumerates all action types.
 */

// Fetch dataset domains metadata from backend
export const FETCH_DATASET_DOMAINS = "fetch_dataset_domains";

// Fetch dataset types metadata from backend
export const FETCH_DATASET_TYPES = "fetch_dataset_types";
